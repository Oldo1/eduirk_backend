services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-change-me}
      POSTGRES_DB: ${DB_NAME:-eduirk_db}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      # Можно убрать, если БД не нужна снаружи
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-eduirk_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-change-me}@db:5432/${DB_NAME:-eduirk_db}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-change-me}
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-eduirk_db}
      SECRET_KEY: ${SECRET_KEY:-change-this-secret-in-dev}
      PD_ENCRYPTION_KEY: ${PD_ENCRYPTION_KEY:-change-this-key-in-dev}
      ENABLE_DEV_TEST_USERS: "${ENABLE_DEV_TEST_USERS:-true}"
    depends_on:
      db:
        condition: service_healthy
    ports:
      # Снаружи 8001 — потому что 8000 занят соседним контейнером (gamehub_backend).
      # При обращении к API используй http://localhost:8001
      - "8001:8000"
    volumes:
      # Persistent certificate assets.
      - ./static/certificates/backgrounds:/app/static/certificates/backgrounds
      - ./static/certificates/facsimiles:/app/static/certificates/facsimiles
      - ./static/certificates/generated:/app/static/certificates/generated

volumes:
  pgdata:
